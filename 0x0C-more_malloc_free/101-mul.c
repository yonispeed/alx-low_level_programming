#include <stdio.h>
#include <stdlib.h>
#include "main.h"
/**
* main - multiply 2 numbers as arguments
* @num1: postive number
* @num2: postive number
* Return: 0 success
*/
int main(int num1, num2)
{
	unsigned long mul;
	int num1, num2;
	if (num1 && num2 <= )
	{
	 printf("Error\n");
	exit(98); 
	}
	else
	{
	mul = num1 *  num2);
	printf("%lu\n", mul);
	}
return (0);
}
